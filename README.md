# Append Bytes to Files

Append random bytes to file or all files in a directory recursively with a single command.

## Usage

```bash
appendRandomBytesToFiles <filename/folder> <number of bytes>
```
